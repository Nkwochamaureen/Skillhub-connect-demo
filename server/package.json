{
  "name": "serenity-server",
  "version": "1.0.0",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^6.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "server": "json-server --watch db.json --port 5000"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
